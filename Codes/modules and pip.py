# Modules are python file that can be imported to the current python file.

# if exponent function is a module to import

import exponent function

# exponent_function.<function name>

# then we can use the functions defined in that module.
# Modules by default stored in the lib folder.

# there are lot of modules available at python docs website. External and built-in modules. And also third party modules.

# modules can also be installed bby using 'pip' in command prompt. Those modules appear in lib/site packages.
# pip install <module name>
# pip uninstall <module name>

